import styled from 'styled-components';

export const DrawAreaWrapper = styled.div`
  display: flex;
  position: relative;
  width: calc(100% - 10px);
  height: 80vh;
  padding: 10px 0 0 10px;
  img {
    margin: auto;
    position: fixed;
  }
`